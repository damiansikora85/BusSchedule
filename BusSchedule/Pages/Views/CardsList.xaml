<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             x:Class="BusSchedule.Pages.Views.CardsList">
    <ContentView.Resources>
        <ResourceDictionary>
            <xct:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        </ResourceDictionary>
    </ContentView.Resources>
  <ContentView.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding Cards}" IsVisible="{Binding HasCards}">

            </ListView>
            <Label Text="Brak dodanych kart" IsVisible="{Binding HasCards, Converter={StaticResource InvertedBoolConverter}}" VerticalOptions="CenterAndExpand" HorizontalTextAlignment="Center"/>
            <Button Text="Dodaj karte" Command="{Binding AddCardCommand}"/>
        </StackLayout>
    </ContentView.Content>
</ContentView>